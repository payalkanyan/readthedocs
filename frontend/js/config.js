/**
 * API Configuration
 *
 * Change this to point to your backend API.
 *
 * Options:
 * - '/api'          → if using nginx proxy (recommended for production)
 * - 'http://localhost:8000'  → for local development
 * - 'https://your-domain.com/api' → for custom domain with proxy
 */

const API_BASE_URL = '/api';
